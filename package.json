{
  "name": "how-much",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run scrape.ts",
    "db-push": "bunx drizzle-kit generate && bunx drizzle-kit migrate",
    "front": "bun run --watch front/server.ts",
    "chrome-debug": "google-chrome --remote-debugging-port=9222 --remote-allow-origins=* --user-data-dir=/tmp/how-much-chrome --no-first-run --no-default-browser-check"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "better-sqlite3": "^12.11.1",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@types/node": "^26.1.1",
    "chalk": "^5.6.2",
    "drizzle-orm": "^0.45.2",
    "playwright": "^1.61.1"
  }
}
